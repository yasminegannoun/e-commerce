{% extends 'base.html.twig' %}

{% block title %}Livres index{% endblock %}

{% block body %}
    <h1>Livres index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Isbn</th>
                <th>Slug</th>
                <th>Image</th>
                <th>Resume</th>
                <th>Editeur</th>
                <th>EditedAt</th>
                <th>Prix</th>
                <th>Qte</th>
                <th>Auteur</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for livre in livres %}
            <tr>
                <td>{{ livre.id }}</td>
                <td>{{ livre.titre }}</td>
                <td>{{ livre.isbn }}</td>
                <td>{{ livre.slug }}</td>
                <td>{{ livre.image }}</td>
                <td>{{ livre.resume }}</td>
                <td>{{ livre.editeur }}</td>
                <td>{{ livre.editedAt ? livre.editedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ livre.prix }}</td>
                <td>{{ livre.Qte }}</td>
                <td>{{ livre.Auteur }}</td>
                <td>
                    <a href="{{ path('app_livre_show', {'id': livre.id}) }}">show</a>
                    <a href="{{ path('app_livre_edit', {'id': livre.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="12">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_livre_new') }}">Create new</a>
{% endblock %}
